# Firebase Configuration for Vercel Deployment
# Copy this file to .env.local for local development
# Add these same variables to your Vercel dashboard under Settings > Environment Variables

VITE_API_KEY=your_firebase_api_key_here
VITE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_PROJECT_ID=your_firebase_project_id
VITE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_APP_ID=your_firebase_app_id

# Important Notes:
# 1. These are PUBLIC environment variables (prefixed with VITE_)
# 2. They will be included in the client-side bundle
# 3. DO NOT put sensitive data here
# 4. Make sure your Vercel domain is added to Firebase Auth authorized domains

# Steps to configure Firebase Auth for Vercel:
# 1. Go to Firebase Console > Authentication > Settings > Authorized domains
# 2. Add your Vercel domain (e.g., your-app-name.vercel.app)
# 3. Make sure Google Auth provider is enabled in Firebase Console
